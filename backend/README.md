# C++ GitHub Profile Viewer

A lightweight C++ backend using cpp-httplib to fetch GitHub user profiles.

## Build
```bash
g++ main.cpp -o server -lpthread
./server
```

## Endpoints
- `GET /` — Health check
